apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-frontend
  namespace: db-stack
  labels:
    app: db-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-frontend
  template:
    metadata:
      labels:
        app: db-frontend
    spec:
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: db-frontend
          # Use the GitHub Container Registry image for the frontend
          image: ghcr.io/dabeastnet/db-frontend:v3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          # Configure probes to give Apache extra time to become ready
          readinessProbe:
            httpGet:
              path: /
              port: http
            # Wait a bit longer before checking readiness since the Apache proxy must also reach the API
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            # Delay the liveness probe to avoid restart loops while Apache starts
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"