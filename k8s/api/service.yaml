apiVersion: v1
kind: Service
metadata:
  name: db-api
  namespace: db-stack
  labels:
    app: db-api
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: db-api